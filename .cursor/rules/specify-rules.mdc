# pndcgn Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-14

## Active Technologies
- Bash (strict mode: `set -euo pipefail`) + pandoc + sqlite3 + shellspec (test) + curl (for extension download) (001-sync-docs-spec)
- SQLite (persistent state/cache) with WAL mode (001-sync-docs-spec)
- Bash 5.0+ (strict mode: `set -euo pipefail`) (001-sync-docs-spec)
- SQLite database at `${XDG_STATE_HOME:-${HOME}/.local/state}/pndcgn/cache.sqlite` (001-sync-docs-spec)
- Bash 5.x (strict mode: `set -euo pipefail`) + fzf (multi-select mode), sqlite3, pandoc (002-multi-dir-select)
- SQLite (existing `cache.sqlite` - extend `runs` table for multi-source) (002-multi-dir-select)
- Bash 5.x (strict mode: `set -euo pipefail`) + fzf (multi-select mode, optional), sqlite3, pandoc (002-multi-dir-select)

- Bash (strict mode: `set -euo pipefail`) + pandoc + sqlite3 + shellspec (test) (001-sync-docs-spec)

## Project Structure

```text
src/
tests/
```

## Commands

# Add commands for Bash (strict mode: `set -euo pipefail`)

## Code Style

Bash (strict mode: `set -euo pipefail`): Follow standard conventions

## Recent Changes
- 002-multi-dir-select: Added Bash 5.x (strict mode: `set -euo pipefail`) + fzf (multi-select mode, optional), sqlite3, pandoc
- 002-multi-dir-select: Added Bash 5.x (strict mode: `set -euo pipefail`) + fzf (multi-select mode), sqlite3, pandoc
- 001-sync-docs-spec: Added Bash 5.0+ (strict mode: `set -euo pipefail`)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
